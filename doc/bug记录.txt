1. 关于jal指令的实现
	本来在实验文档中，jal指令只有两个阶段“if，id”
	但是考虑到，jal指令需要写存储器，故给他增加了一个阶段WD
	这也是希望，每一个指令的各个阶段能够制作一样事情，若是一个阶段同时做多件事情很容易会混乱
	如，如果让jal指令在id阶段就写寄存器
	第一个时钟上升沿，control_unit更新，PC可改变，下降沿，PC改变，读取指令成功
	第二个时钟上升沿，control_unit更新，此时若对jal指令有特例，寄存器可写，到了时钟下降沿，IR模块写入新的指令，同时寄存器写入IR模块之前输出的值（即上一个指令的IR阶段）